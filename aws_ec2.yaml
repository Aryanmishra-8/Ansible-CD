plugin: amazon.aws.aws_ec2
regions:
  - ap-south-1   

filters:
  instance-state-name: running
  "tag:Name": postgresql  

hostnames:
  - public_ip_address 

keyed_groups:
  - key: tags.Name
    prefix: ""

compose:
  ansible_host: public_ip_address

